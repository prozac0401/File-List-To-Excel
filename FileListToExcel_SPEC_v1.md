# File List to Excel — 기능 명세서 v1.0

- 문서일: 2026-09-24
- 대상 환경: Windows 11 + Microsoft Excel Desktop
- 목적: Windows 탐색기에서 현재 선택한 파일/폴더를 별도 입력 과정 없이 즉시 Excel 표로 변환한다.
- 제품 철학: **선택 자체가 입력이다. 실행은 한 번, 결과는 익숙한 도구에서.**

---

## 1. 제품 한 줄 정의

Windows 탐색기에서 파일 또는 폴더를 선택한 뒤 우클릭 메뉴 한 번으로, 선택 대상의 파일 정보를 필터·정렬·가공 가능한 Excel 통합문서로 즉시 열어주는 경량 도구.

```text
탐색기에서 선택
    ↓
우클릭 → 파일 목록을 Excel로
    ↓
Excel 표 즉시 생성/열기
```

이 도구는 파일 관리 프로그램이 아니다. 파일 시스템의 정보를 **Excel이라는 범용 작업대로 빠르게 투영하는 연결 도구**다.

---

## 2. 해결하려는 문제

실무에서는 다음과 같은 상황이 반복된다.

- 폴더 안에 어떤 파일이 있는지 표로 정리해야 한다.
- 여러 제출 파일의 이름·크기·수정일을 한눈에 확인해야 한다.
- 파일 목록을 다른 명단과 대조해야 한다.
- 자료 폴더의 현황을 다른 사람에게 전달해야 한다.
- 파일명이나 경로를 하나씩 복사해 Excel에 붙이는 일이 발생한다.
- 하위 폴더 구조를 포함한 파일 목록을 만들어 필터링하고 싶다.

기존 방식에서는 `dir`/PowerShell/탐색기 복사/Excel 붙여넣기/수작업 정리가 섞인다.

이 도구의 목표는 이를 다음 한 줄로 줄이는 것이다.

> **대상을 이미 선택했다면, 추가 입력 없이 Excel로 보여준다.**

---

## 3. 핵심 설계 원칙

### 3.1 선택이 곧 입력

사용자는 프로그램 안에서 파일을 다시 선택하지 않는다.

- 탐색기에서 파일을 선택했다면 → 선택한 파일이 입력
- 폴더를 우클릭했다면 → 해당 폴더가 입력
- 폴더 내부 빈 공간에서 실행했다면 → 현재 폴더가 입력

파일 선택 대화상자를 다시 띄우지 않는다.

### 3.2 기본 실행에는 설정창이 없다

정상적인 사용 흐름에서 다음 단계가 없어야 한다.

- 프로젝트 등록
- 프로필 선택
- 옵션 설정
- 저장 위치 선택
- 출력 형식 선택
- 프롬프트 입력

기본 명령을 누르면 바로 Excel을 생성한다.

### 3.3 결과 UI를 새로 만들지 않는다

목록 조회·필터·정렬·추가 메모·후속 가공은 Excel이 이미 잘한다.

따라서 별도의 결과 Viewer를 만들지 않는다.

### 3.4 빠른 기본 목록과 느린 상세 분석을 분리한다

기본 기능은 파일 내용을 열지 않고 파일 시스템 메타데이터만 읽는다.

PDF 페이지 수, PPT 슬라이드 수, XLSX 시트 수 같은 정보는 v1 기본 경로에서 제외한다.

### 3.5 상주하지 않는다

- Tray app 없음
- Background service 없음
- 계정 없음
- 자체 데이터베이스 없음

우클릭 호출 시 helper 프로세스가 실행되고 Excel을 연 뒤 종료한다.

---

# 4. 사용자 진입점

## 4.1 파일 하나 이상 선택

Windows 탐색기에서 파일 하나 이상을 선택하고 우클릭한다.

메뉴:

```text
파일 목록을 Excel로
```

동작:

- 선택한 파일만 한 행씩 Excel에 기록
- 선택하지 않은 같은 폴더의 파일은 포함하지 않음
- 여러 폴더에서 동시에 선택 가능한 Explorer 환경이라면 각 실제 경로를 그대로 수집

---

## 4.2 폴더 하나 이상 선택

메뉴:

```text
폴더 내용 목록을 Excel로
하위 폴더까지 Excel로
```

### `폴더 내용 목록을 Excel로`

- 선택된 폴더 각각의 **직접 자식 항목**만 수집
- 재귀 탐색하지 않음
- 파일과 폴더 모두 목록에 포함

### `하위 폴더까지 Excel로`

- 선택된 폴더의 전체 하위 트리를 재귀 탐색
- 기본적으로 파일을 출력
- 폴더 행 포함 여부는 v1에서는 `포함`으로 고정해도 되나, 구현 복잡도/가독성 평가 후 파일만 출력하는 안도 허용
- `상대경로`, `깊이` 열을 반드시 포함

---

## 4.3 폴더 내부 빈 공간 우클릭

현재 탐색기 폴더를 대상으로 한다.

메뉴:

```text
현재 폴더 목록을 Excel로
현재 폴더 + 하위 폴더를 Excel로
```

사용자가 탐색 중인 현재 위치를 그대로 입력으로 사용한다.

---

# 5. 출력 Excel 명세

## 5.1 기본 워크북 구조

기본적으로 아래 시트를 생성한다.

```text
[Files]
[Errors]   ← 오류가 있을 때만 생성해도 됨
[Info]     ← 선택 사항. v1에서는 생략 가능
```

핵심 시트는 `Files`다.

---

## 5.2 Files 시트 — 기본 열

권장 순서:

| 열 | 표시명 | 데이터 형식 | 설명 |
|---|---|---|---|
| A | 이름 | Text | 파일/폴더 이름 |
| B | 종류 | Text | 확장자 또는 `폴더` |
| C | 크기 | Number | byte 단위 실제 숫자 |
| D | 크기(표시) | Text | KB/MB/GB 사람이 읽기 쉬운 표시 |
| E | 수정일 | Excel DateTime | 마지막 수정 시각 |
| F | 생성일 | Excel DateTime | 생성 시각 |
| G | 상위 폴더 | Text | 바로 위 폴더 이름 |
| H | 상대경로 | Text | 기준 폴더 대비 상대경로 |
| I | 전체경로 | Text | 절대 경로 |
| J | 깊이 | Number | 기준 폴더 아래 depth |
| K | 속성 | Text | Hidden/ReadOnly 등 필요한 최소 정보 |

### 필수 열

v1 필수:

- 이름
- 종류
- 크기
- 수정일
- 상위 폴더
- 상대경로
- 전체경로

생성일/깊이/속성은 구현 우선순위에 따라 v1 포함 여부를 조정할 수 있다.

---

## 5.3 파일/폴더 열기

`이름` 셀은 가능하면 Hyperlink로 작성한다.

- 파일 → 해당 파일 열기
- 폴더 → 해당 폴더 열기

Excel 보안 정책이나 Hyperlink 제약으로 이름 셀에 직접 연결이 어려우면 별도 `열기` 열을 추가할 수 있다.

---

## 5.4 Excel Table 적용

`Files` 범위는 Excel Table로 생성한다.

필수:

- 첫 행 Header
- AutoFilter 활성화
- Freeze top row
- 적절한 열 너비
- 날짜 열은 날짜/시간 값으로 저장
- 크기 열은 문자열이 아닌 숫자로 저장
- 전체경로 열은 넓이를 과도하게 차지하지 않도록 뒤쪽에 배치

### 정렬 가능성

다음이 정확히 숫자/날짜 기준으로 정렬되어야 한다.

- 파일 크기
- 수정일
- 생성일
- 깊이

`12 MB`, `800 KB` 같은 문자열만 저장하여 문자열 정렬되는 방식은 허용하지 않는다.

---

# 6. 다중 폴더 처리

사용자가 여러 폴더를 동시에 선택하면 각 항목이 어느 입력 폴더에서 왔는지 식별할 수 있어야 한다.

필요 시 다음 열을 추가한다.

```text
기준 폴더
```

예:

| 기준 폴더 | 상대경로 | 이름 |
|---|---|---|
| 과정A | 01_OT\OT자료.pptx | OT자료.pptx |
| 과정B | 01_OT\OT자료.pptx | OT자료.pptx |

여러 기준 폴더를 다루더라도 상대경로는 각 기준 폴더를 root로 계산한다.

---

# 7. 실행 흐름

## 7.1 일반 파일 목록

```text
사용자가 파일 선택
→ 우클릭
→ "파일 목록을 Excel로"
→ helper 실행
→ Explorer selection 전달
→ metadata 수집
→ 임시 xlsx 생성
→ Excel로 열기
→ helper 종료
```

정상적인 경우 별도 UI를 띄우지 않는다.

---

## 7.2 하위 폴더 재귀 목록

```text
폴더 선택
→ "하위 폴더까지 Excel로"
→ 재귀 탐색
→ 파일 수가 작으면 바로 진행
→ 큰 경우에만 작은 Progress UI
→ xlsx 생성
→ Excel 열기
```

---

# 8. 임시 파일 및 저장 정책

## 기본 정책

실행 때마다 사용자에게 저장 위치를 묻지 않는다.

임시 xlsx를 생성한 뒤 Excel에서 즉시 연다.

예:

```text
%TEMP%\FileListToExcel\FileList_20260924_094322.xlsx
```

사용자는 필요하면 Excel에서 `Ctrl+S` 또는 `다른 이름으로 저장`을 사용한다.

### 이유

- 원본 폴더에 `파일목록.xlsx`가 계속 생성되는 것을 방지
- 저장 여부를 결정하기 위한 중간 질문 제거
- 일회성 검토를 빠르게 수행

### 임시 파일 정리

- 일정 기간 이상 지난 임시 결과 자동 정리 가능
- v1에서는 Windows TEMP 정리 정책에 맡기는 것도 허용
- 열린 파일을 강제로 삭제하지 않음

---

# 9. 성능 요구사항

## 9.1 기본 목록

파일 내용을 열지 않는다.

사용하는 정보는 원칙적으로 파일 시스템에서 빠르게 얻을 수 있는 메타데이터로 제한한다.

목표:

- 수백 개: 즉시 체감
- 수천 개: 별도 설정 없이 처리
- 10,000개: 실사용 가능
- 수만 개 이상: Progress 제공

정확한 초 단위 SLA보다 **사용자가 기다리는 동안 프로그램이 멈춘 것처럼 보이지 않는 것**을 우선한다.

---

## 9.2 Progress UI 표시 기준

작업 시작 전 전체 개수를 알 수 없는 재귀 탐색 특성상 다음 중 하나로 구현한다.

### 방식 A — 시간 기반

작업이 500~800ms 이상 지속되면 Progress 창 표시.

### 방식 B — 항목 기반

예: 2,000개 이상 발견되면 Progress 표시.

권장: 시간 기반 + 발견 건수 표시.

```text
파일 목록 생성 중...

발견: 18,421개
현재: ...\03_Result\...

[취소]
```

---

# 10. 오류 처리

오류 하나 때문에 전체 작업을 중단하지 않는다.

예:

- Access Denied
- 지나치게 긴 경로
- 삭제/변경 중인 파일
- 네트워크 드라이브 일시 실패
- 손상된 reparse point

가능한 항목은 계속 수집한다.

오류는 `Errors` 시트에 기록한다.

| 경로 | 오류 | 상세 |
|---|---|---|
| C:\...\Restricted | Access denied | ... |

완료 후 전체 작업 실패 팝업 대신 필요할 경우 가벼운 상태만 표시한다.

```text
목록 8,421개 생성 완료
3개 항목은 읽지 못했습니다.
```

---

# 11. OneDrive / 네트워크 / Placeholder 정책

## OneDrive

목록 생성을 위해 cloud-only placeholder 파일을 다운로드하지 않는다.

가능한 파일 시스템 메타데이터만 기록한다.

향후 필요하면 다음 상태 열을 추가할 수 있다.

```text
로컬 상태
- Local
- Cloud only
- Always available
```

단, v1 필수 아님.

## 네트워크 드라이브

- 일반 파일 시스템과 동일하게 처리
- 느린 경로 때문에 UI가 얼지 않도록 worker thread 사용
- 연결 실패는 Errors에 기록

---

# 12. 파일 유형별 상세 분석 — v1 제외

아래 기능은 아이디어 가치가 있으나 **기본 파일 목록 기능에는 넣지 않는다.**

- PDF 페이지 수
- PowerPoint 슬라이드 수
- Excel Sheet 수
- Word 페이지/문단 수
- 이미지 해상도/EXIF
- ZIP 내부 파일 수
- Hash 계산
- 파일 내용 검색

이러한 기능은 파일을 실제로 열거나 추가 파싱해야 하므로 기본 명령의 속도와 단순성을 해친다.

향후 별도 명령으로 확장 가능:

```text
상세 문서 목록을 Excel로
```

---

# 13. Windows Explorer 통합

## 13.1 우클릭 메뉴 설계

메뉴가 지나치게 늘어나지 않도록 권장 구조는 다음과 같다.

### 방법 A — 상위 메뉴 1개

```text
빠른 파일 도구 >
    파일 목록을 Excel로
    하위 폴더까지 Excel로
```

### 방법 B — 직접 노출

```text
파일 목록을 Excel로
```

v1에서는 사용 빈도가 매우 높을 것을 전제로 직접 노출을 우선 검토한다.

다만 Windows 11의 modern context menu 등록 제약 및 구현 비용에 따라 classic context menu 또는 `더 많은 옵션 표시` 하위에 들어갈 수 있다.

구현 시 **설치 안정성과 제거 용이성**을 우선한다.

---

# 14. 설치 / 제거

## 설치 요구사항

- 1회 설치
- 관리자 권한은 꼭 필요한 경우에만 사용
- Excel Add-in 설치 금지
- Explorer extension은 최소 침습적으로 구현
- 별도 runtime 설치 가능성 최소화

설치 결과:

```text
Program Files 또는 사용자 LocalAppData
+ Explorer context menu registration
```

## 제거 요구사항

제거 시:

- 프로그램 파일 삭제
- 우클릭 메뉴 등록 제거
- 불필요한 shell 등록 흔적 제거
- 사용자 Excel 파일은 건드리지 않음
- 임시 결과 파일은 강제 삭제하지 않아도 됨

다른 Office 프로그램이나 기존 Explorer extension에 영향을 주지 않아야 한다.

---

# 15. 권장 기술 구조

구현 언어는 확정하지 않지만 구조적으로 다음 컴포넌트를 분리한다.

```text
Explorer Integration
        ↓
CLI / Helper Entry
        ↓
Selection Resolver
        ↓
File Enumerator
        ↓
Metadata Collector
        ↓
Workbook Writer
        ↓
Excel Launcher
```

### Explorer Integration

선택 파일/폴더 path를 helper에 전달한다.

### Selection Resolver

- 파일 선택
- 폴더 선택
- 현재 폴더
- 다중 선택

을 하나의 내부 입력 모델로 정규화한다.

### File Enumerator

- direct children
- recursive

두 모드 지원.

### Metadata Collector

가능한 한 OS 파일 메타데이터만 읽는다.

### Workbook Writer

Excel COM 자동화보다 우선적으로 **xlsx를 직접 생성할 수 있는 라이브러리** 사용을 검토한다.

이유:

- Excel을 열기 전 결과 생성 가능
- Excel COM 프로세스 잔류 문제 방지
- 생성 로직 테스트 용이

완성된 xlsx만 기본 연결 프로그램 또는 Excel executable로 연다.

---

# 16. 기본 데이터 모델

예시 내부 모델:

```text
FileEntry
- name
- extension
- is_directory
- size_bytes
- created_at
- modified_at
- parent_name
- absolute_path
- relative_path
- depth
- attributes
- root_label
```

오류 모델:

```text
ScanError
- path
- error_type
- message
```

기본 목록 로직과 Excel 출력 로직을 분리하여 테스트 가능하게 한다.

---

# 17. v1 범위

## 반드시 포함

- [ ] 선택 파일 목록 → Excel
- [ ] 선택 폴더 직접 내용 → Excel
- [ ] 선택 폴더 재귀 목록 → Excel
- [ ] 현재 폴더 내용 → Excel
- [ ] 다중 선택
- [ ] 이름/종류/크기/수정일/상대경로/전체경로
- [ ] Excel Table
- [ ] Filter
- [ ] Freeze Header
- [ ] Hyperlink
- [ ] 임시 xlsx 생성 후 자동 열기
- [ ] 대량 처리 Progress
- [ ] Cancel
- [ ] 오류 지속 처리 + Errors 시트
- [ ] 설치/제거

## 포함하면 좋은 항목

- [ ] 생성일
- [ ] 깊이
- [ ] Hidden/ReadOnly 속성
- [ ] 다중 root 구분

## v1 제외

- [ ] PDF 페이지 수
- [ ] PPT 슬라이드 수
- [ ] Excel Sheet 수
- [ ] 이미지 EXIF
- [ ] Hash
- [ ] 파일 내용 분석
- [ ] AI 기능
- [ ] 사용자 계정
- [ ] Cloud sync
- [ ] 자체 Viewer
- [ ] 파일 변경/삭제/이동
- [ ] Excel에서 원본 파일 시스템으로 역반영

---

# 18. 수용 기준 (Acceptance Criteria)

## AC-01 선택 파일

서로 다른 확장자의 파일 20개를 선택한 뒤 `파일 목록을 Excel로` 실행하면 20개 행이 생성되어야 한다.

## AC-02 선택하지 않은 파일 제외

같은 폴더에 100개 파일이 있어도 3개만 선택했다면 결과에는 선택한 3개만 나타나야 한다.

## AC-03 폴더 직접 내용

폴더 직접 아래의 파일/폴더만 생성되며 하위 폴더 안의 파일은 포함되지 않아야 한다.

## AC-04 재귀

`하위 폴더까지` 실행 시 모든 접근 가능한 하위 항목이 상대경로와 함께 나타나야 한다.

## AC-05 크기 정렬

Excel에서 `크기` 열을 정렬했을 때 실제 byte 크기 순으로 정확히 정렬되어야 한다.

## AC-06 날짜 정렬

수정일은 문자열이 아니라 Excel DateTime으로 생성되어 날짜 정렬이 가능해야 한다.

## AC-07 Hyperlink

파일 이름 또는 열기 링크를 클릭하면 원본 항목을 열 수 있어야 한다.

## AC-08 오류 격리

한 폴더에서 Access Denied가 발생해도 나머지 파일 목록 생성은 계속되어야 한다.

## AC-09 대량 파일

10,000개 수준의 재귀 목록 생성 시 프로그램이 `응답 없음` 상태로 장시간 보이지 않아야 하며 진행상황을 확인할 수 있어야 한다.

## AC-10 Cancel

대량 탐색 도중 취소하면 안전하게 중단되어야 하며 불완전한 Excel이 무조건 열리지 않아야 한다.

## AC-11 설치 제거

제거 후 Explorer 우클릭 메뉴가 남지 않아야 한다.

## AC-12 비상주

일반 사용 종료 후 전용 background process가 계속 실행되고 있지 않아야 한다.

---

# 19. 테스트 시나리오

최소 다음 환경을 테스트한다.

1. 파일 1개
2. 파일 100개
3. 파일 10,000개
4. 빈 폴더
5. 여러 폴더 선택
6. 한글/영문/숫자/공백 파일명
7. 매우 긴 파일명과 경로
8. 확장자 없는 파일
9. 숨김 파일
10. 읽기 전용 파일
11. 네트워크 드라이브
12. OneDrive cloud-only 항목
13. 접근 권한 없는 하위 폴더
14. 탐색 중 파일이 삭제되는 경우
15. Excel이 이미 실행 중인 경우
16. Excel이 설치되지 않은 PC
17. 생성된 xlsx를 사용자가 저장하지 않고 닫는 경우
18. 우클릭 직후 Explorer가 종료/재시작되는 경우

---

# 20. Excel 미설치 환경

본 도구의 주 대상은 Excel Desktop 설치 환경이다.

Excel이 설치되지 않은 경우 정책 후보:

### v1 권장

- xlsx는 정상 생성
- OS 기본 xlsx 연결 앱으로 열기 시도
- 연결 앱도 없으면 생성 파일 위치를 알려줌

별도의 spreadsheet viewer를 제공하지 않는다.

---

# 21. 제품 가치 판단 기준

이 도구는 기능 수보다 **호출 비용이 거의 0이라는 점**이 핵심이다.

성공 여부를 다음 질문으로 평가한다.

> 파일 목록이 필요하다는 생각이 든 뒤 Excel 표를 보기까지 몇 번의 행동이 필요한가?

목표:

```text
선택 → 우클릭 → 실행
```

또는 이미 폴더 안에 있다면:

```text
우클릭 → 실행
```

여기에 추가 설정창이 상시 끼어들면 설계가 실패한 것으로 본다.

---

# 22. 동일 계열 도구로 확장 가능한 아이디어

본 제품을 거대한 Toolbox 앱으로 합치기보다, 동일한 **Selection → Artifact** 철학을 공유하는 작은 기능들을 독립적으로 추가할 수 있다.

## 우선 검토 후보

### A. 두 폴더 비교 → Excel

```text
폴더 2개 선택
→ 우클릭
→ 두 폴더 비교표
```

결과:

- 양쪽 동일
- A에만 존재
- B에만 존재
- 이름은 같지만 크기/수정일이 다른 파일

기본은 빠른 메타데이터 비교, 필요 시 Hash 모드 추가.

---

### B. 폴더 용량 현황 → Excel

```text
폴더 우클릭
→ 용량 현황을 Excel로
```

결과:

- 하위 폴더별 파일 수
- 하위 폴더 수
- 총 용량
- 최근 수정일

목적: 오래된 업무 폴더/디스크 정리.

---

### C. 폴더 구조 → Clipboard

```text
폴더 우클릭
→ 폴더 구조 복사
```

결과:

```text
Project/
├─ src/
│  ├─ app.py
│  └─ parser.py
├─ tests/
└─ README.md
```

개발 문서, README, ChatGPT/Codex context에 바로 붙여넣기.

---

### D. 압축파일 목록 → Excel

```text
ZIP 여러 개 선택
→ 압축파일 내용 Excel로
```

압축 해제하지 않고:

- ZIP 이름
- 내부 상대경로
- 파일명
- 내부 파일 크기

를 표로 생성.

---

### E. 문서 현황 → Excel

Office/PDF 파일 여러 개 선택 후 상세 메타정보를 표로 생성.

- PPTX: 슬라이드 수
- XLSX: 시트 수
- PDF: 페이지 수
- DOCX: 기본 문서 정보

기본 `파일 목록 → Excel`과 분리하여 느린 상세 분석 기능으로 제공.

---

### F. 파일 Manifest → Excel

전달/백업 전후 검증 목적.

- 파일명
- 상대경로
- 크기
- 수정일
- SHA-256

Hash 때문에 느릴 수 있으므로 별도 명령으로 둔다.

---

# 23. 하지 말아야 할 방향

이 도구를 발전시키면서 다음 방향은 경계한다.

## 범용 파일 관리자화

- 파일 이동 UI
- 삭제 UI
- Rename editor
- 미리보기 pane
- 자체 검색

→ Windows Explorer와 중복된다.

## 범용 업무 플랫폼화

- Workflow editor
- Recipe builder
- AI prompt 입력
- Project database

→ 호출 비용이 커지고 원래 장점을 잃는다.

## 모든 정보를 한 번에 분석

파일 목록을 만들 때 Office 내부 구조, PDF, 이미지, Hash까지 전부 읽으면 단순한 기본 동작이 느려진다.

**기본 명령은 언제나 빠르고 예측 가능해야 한다.**

---

# 24. 구현 우선순위 제안

## Phase 1 — CLI Prototype

Explorer 통합 전에 core부터 검증한다.

```text
filelist.exe --files <paths...>
filelist.exe --folder <path>
filelist.exe --recursive <path>
```

검증:

- metadata 수집
- 상대경로 계산
- xlsx 생성
- Excel Table
- Hyperlink

## Phase 2 — Explorer Context Menu

- 파일 선택 전달
- 폴더 선택 전달
- 현재 폴더 전달
- 설치/삭제

## Phase 3 — 대량/예외 처리

- progress
- cancel
- network path
- OneDrive
- access denied

## Phase 4 — UX polishing

- 열 순서
- Column width
- 임시파일 관리
- 에러 표현
- 여러 root 표기

## Phase 5 — 선택적 sibling tools

- 두 폴더 비교
- 폴더 구조 복사
- 용량 현황

본체를 비대하게 만들지 않고 같은 실행철학을 가진 명령으로 확장한다.

---

# 25. Codex 전달용 요약

다음 원칙을 구현 내내 유지한다.

1. **Explorer selection이 입력이다.** 프로그램에서 다시 선택하게 하지 않는다.
2. 정상 실행에는 설정창이 없어야 한다.
3. 기본 기능은 파일 내용을 열지 않는다.
4. 결과는 자체 UI가 아니라 Excel Table로 제공한다.
5. 결과는 임시 xlsx로 즉시 열고 저장 여부는 사용자에게 맡긴다.
6. 오류 하나가 전체 스캔을 중단하지 않는다.
7. 대량 작업만 progress/cancel UI를 사용한다.
8. 상주 프로세스를 두지 않는다.
9. 설치·제거 후 Explorer에 흔적이 남지 않아야 한다.
10. **Selection → one action → useful artifact**라는 제품 철학을 깨는 기능은 넣지 않는다.

---

## 최종 목표 UX

```text
[탐색기]

홍길동_결과.zip
김철수_결과.zip
이영희_결과.zip
        ↑ 3개 선택

우클릭
  → 파일 목록을 Excel로

[Excel]

이름              종류   크기      수정일      상대경로
홍길동_결과.zip    zip    31 MB     09/23 ...   ...
김철수_결과.zip    zip    28 MB     09/23 ...   ...
이영희_결과.zip    zip    35 MB     09/24 ...   ...
```

**여기까지 사용자에게 필요한 설명은 사실상 없다.**
