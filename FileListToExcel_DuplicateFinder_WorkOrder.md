# File List to Excel — 중복 파일 찾기 기능 추가 작업지시서

- 문서 버전: v1.0
- 작성일: 2026-09-24
- 대상: 기존 **File List to Excel** 프로젝트
- 목적: 기존의 “선택한 파일/폴더를 Excel 목록으로 만드는 기능”에 **같은 파일 찾기 / 중복 파일 찾기** 기능을 추가한다.

---

# 1. 이번 작업의 목표

기존 도구의 장점은 다음과 같다.

> **탐색기에서 이미 선택한 것이 입력이 되고, 우클릭 한 번으로 바로 결과를 얻는다.**

이번 기능도 이 원칙을 그대로 지켜야 한다.

새 기능은 파일을 삭제하거나 정리하는 프로그램이 아니다.

**같은 내용의 파일이 어디에 있는지 빠르게 찾아서, 사람이 확인하기 쉬운 Excel 결과로 보여주는 기능**이다.

기본 사용 흐름은 다음과 같다.

```text
파일 또는 폴더 선택
    ↓
우클릭
    ↓
같은 파일 찾기 / 중복 파일 찾기
    ↓
빠른 후보 추리기
    ↓
필요한 파일만 실제 내용 비교
    ↓
Excel 결과 열기
```

---

# 2. 반드시 지켜야 할 제품 원칙

## 2.1 추가 입력을 최소화한다

정상적인 사용에서 아래 창을 매번 띄우지 않는다.

- 검색할 파일 다시 선택
- 검색 옵션 선택
- Hash 종류 선택
- 저장 위치 선택
- Excel 열 선택

탐색기에서 선택한 대상이 곧 입력이어야 한다.

---

## 2.2 원본 파일을 변경하지 않는다

v1에서는 다음 기능을 구현하지 않는다.

- 파일 삭제
- 파일 이동
- 중복 자동 정리
- 오래된 파일 자동 선택
- “이 파일을 지우세요” 같은 자동 판단

결과는 **찾아서 보여주는 것**까지만 한다.

삭제나 이동은 사용자가 직접 판단한다.

---

## 2.3 모든 파일의 Hash를 무조건 계산하지 않는다

폴더 안의 파일을 전부 SHA-256으로 읽는 방식은 금지한다.

파일 개수가 많거나 대용량 파일이 있으면 지나치게 느려질 수 있기 때문이다.

먼저 **값싼 정보로 후보를 줄이고**, 정말 비교가 필요한 파일만 읽는다.

---

# 3. 사용자에게 제공할 기능

## 3.1 파일 하나 선택 — `같은 파일 찾기`

### 사용자 동작

```text
파일 하나 선택
→ 우클릭
→ 같은 파일 찾기
```

### 검색 범위

v1에서는 선택한 파일이 들어 있는 **현재 폴더와 그 하위 폴더 전체**를 검색한다.

예:

```text
D:\업무\AWS\운영안.docx
```

을 선택했다면 기본 검색 범위는:

```text
D:\업무\AWS\
```

아래 전체이다.

> 향후 버전에서는 “검색 기준 폴더 지정” 기능을 추가할 수 있도록 내부 구조는 확장 가능하게 만든다. 하지만 v1 실행 때 검색 폴더를 매번 묻지 않는다.

### 결과

선택한 파일과 **내용이 완전히 같은 파일**만 결과에 표시한다.

파일명은 달라도 된다.

예:

```text
교육운영안_최종.docx
교육운영안(1).docx
운영안_backup.docx
```

내용이 같으면 같은 파일로 판단한다.

---

## 3.2 폴더 선택 — `중복 파일 찾기`

### 사용자 동작

```text
폴더 선택
→ 우클릭
→ 중복 파일 찾기
```

### 동작

선택한 폴더와 모든 하위 폴더를 검사하고, 내용이 같은 파일들을 그룹으로 묶는다.

예:

```text
중복 그룹 1
- A\교육운영안.docx
- Backup\교육운영안_최종.docx
- Old\운영안.docx

중복 그룹 2
- result\list.xlsx
- temp\list_copy.xlsx
```

---

## 3.3 여러 폴더 선택

여러 폴더가 선택된 경우에는 선택한 모든 폴더를 하나의 검색 범위로 본다.

즉 서로 다른 선택 폴더 사이에 존재하는 중복도 찾는다.

예:

```text
2025\
2026\
Backup\
```

세 폴더 선택 → `중복 파일 찾기`

→ 세 폴더를 합쳐서 중복 검사.

---

## 3.4 여러 파일 선택 — `선택 파일 중 중복 찾기`

가능하면 v1에 포함한다.

```text
파일 여러 개 선택
→ 우클릭
→ 선택 파일 중 중복 찾기
```

이 경우 폴더 전체를 탐색하지 않고 **선택한 파일끼리만** 비교한다.

이 기능은 빠르게 동작해야 한다.

---

# 4. 중복 판단 기준

중복 파일의 최종 기준은 다음과 같다.

> **파일의 실제 내용이 완전히 같은가?**

다음 정보는 최종 판단 기준이 아니다.

- 파일명
- 확장자
- 생성일
- 수정일
- 저장된 폴더

예:

```text
A.docx
B.docx
```

파일명이 달라도 실제 byte 내용이 같으면 같은 파일이다.

반대로:

```text
A.docx
A - 복사본.docx
```

이름이 비슷하더라도 내용이 다르면 중복이 아니다.

---

# 5. 성능을 위한 단계별 비교 방법

가장 중요한 구현 부분이다.

## 5.1 1단계 — 파일 크기로 후보 줄이기

모든 파일의 `size_bytes`를 읽는다.

파일 크기가 서로 다르면 내용도 같을 수 없으므로 즉시 제외한다.

```text
전체 10,000개
    ↓
크기가 같은 파일이 2개 이상인 그룹만 남김
```

크기가 혼자인 파일은 그 시점에서 검사 종료.

---

## 5.2 2단계 — 빠른 지문(Quick Fingerprint)

파일 크기가 같은 후보가 여러 개 있을 때만 수행한다.

대용량 파일 전체를 바로 읽지 않는다.

권장 규칙:

### 1MB 이하 파일

전체 파일을 바로 SHA-256 계산해도 된다.

### 1MB 초과 파일

다음 위치의 일부만 읽는다.

- 파일 시작 64KB
- 파일 중간 64KB
- 파일 끝 64KB

이 세 부분 + 파일 크기를 이용해 `quick fingerprint`를 만든다.

예:

```text
SHA256(
  size_bytes
  + first_64KB
  + middle_64KB
  + last_64KB
)
```

Quick Fingerprint가 다르면 바로 다른 파일로 판단한다.

---

## 5.3 3단계 — 전체 SHA-256

아래 조건을 모두 통과한 파일만 전체 SHA-256을 계산한다.

```text
파일 크기 같음
AND
Quick Fingerprint 같음
```

전체 SHA-256까지 같으면 최종적으로 같은 파일로 판단한다.

```text
size
  ↓
quick fingerprint
  ↓
full SHA-256
  ↓
duplicate group
```

---

# 6. Hash 계산 구현 세부사항

## 6.1 알고리즘

최종 Hash는 **SHA-256**을 사용한다.

이유:

- 표준 라이브러리로 구현하기 쉽다.
- 충돌 위험이 충분히 낮다.
- 별도 라이브러리 의존성을 추가하지 않아도 되는 경우가 많다.

기존 프로젝트 언어에서 제공하는 표준 SHA-256 API를 우선 사용한다.

---

## 6.2 파일 읽기 방식

파일 전체를 메모리에 올리지 않는다.

Stream 방식으로 일정 크기씩 읽는다.

권장 chunk 크기:

```text
4MB
```

환경에 따라 1~8MB 범위에서 조정 가능하다.

---

## 6.3 동시 Hash 계산

디스크는 CPU와 달리 작업을 많이 병렬화한다고 항상 빨라지지 않는다.

기본 worker 수를 과도하게 늘리지 않는다.

권장:

```text
로컬 SSD: 2~4 workers
HDD / 네트워크 경로: 1~2 workers
```

처음 구현에서는 안전하게 2 worker부터 시작해도 된다.

성능 테스트 후 조정한다.

---

# 7. Hash Cache

반복 실행 성능을 위해 Hash 결과를 로컬에 저장한다.

## 7.1 저장 위치

예:

```text
%LOCALAPPDATA%\FileListToExcel\hash_cache.sqlite
```

JSON 파일보다 SQLite를 권장한다.

파일 수가 많아져도 조회와 정리가 쉽기 때문이다.

---

## 7.2 Cache Key

최소 다음 값을 사용한다.

```text
absolute_path
size_bytes
modified_time_ns
hash_algorithm
```

저장 값:

```text
quick_hash
full_sha256
last_checked_at
```

파일 경로 + 크기 + 수정시간이 이전과 같으면 기존 Hash를 재사용할 수 있다.

하나라도 달라졌다면 다시 계산한다.

---

## 7.3 Cache 주의사항

Cache는 성능 향상을 위한 것이다.

캐시가 손상되거나 삭제되어도 프로그램이 정상 동작해야 한다.

```text
Cache 없음
→ 조금 느리지만 정상 검사
```

이어야 한다.

---

# 8. OneDrive / Cloud Placeholder

중복 검사를 위해 cloud-only 파일을 강제로 다운로드하면 안 된다.

파일 읽기를 시도하기 전에 Windows의 offline / placeholder 속성을 가능한 범위에서 확인한다.

cloud-only 상태로 실제 내용이 로컬에 없는 파일은 기본적으로 Hash 검사에서 제외한다.

결과 또는 Errors/Skipped 시트에 다음처럼 기록한다.

```text
건너뜀: Cloud-only 파일
```

사용자가 중복 검사를 실행했다는 이유만으로 대량의 OneDrive 파일이 내려받아지는 일이 없어야 한다.

---

# 9. Junction / Symbolic Link / Reparse Point

하위 폴더 재귀 검색 시 무한 루프를 방지해야 한다.

v1 기본 정책:

- Directory Junction: 따라가지 않음
- Directory Symbolic Link: 따라가지 않음
- 기타 Reparse Point: 재귀 탐색 대상에서 제외

필요하면 `Skipped` 또는 로그에 기록한다.

---

# 10. 결과 Excel 명세

기존 File List to Excel의 workbook 생성 코드를 최대한 재사용한다.

별도의 Viewer를 만들지 않는다.

---

## 10.1 폴더 중복 검사 결과

시트명:

```text
Duplicates
```

권장 열:

| 열 | 내용 |
|---|---|
| 그룹 | 중복 그룹 번호 |
| 그룹 파일수 | 같은 내용의 파일 개수 |
| 중복 절약 가능 크기 | `(그룹 파일수 - 1) × 파일크기` |
| 이름 | 파일명 |
| 종류 | 확장자 |
| 크기 | byte 숫자 |
| 크기(표시) | KB/MB/GB |
| 수정일 | Excel DateTime |
| 상대경로 | 기준 폴더 아래 경로 |
| 전체경로 | 절대 경로 |
| SHA-256 | 최종 Hash |

`SHA-256`은 오른쪽 끝에 배치한다.

사람이 보기 좋은 열이 먼저 나오게 한다.

---

## 10.2 같은 파일 찾기 결과

시트명:

```text
Matches
```

첫 행 또는 상단 정보 영역에 기준 파일을 표시한다.

예:

```text
기준 파일: D:\업무\AWS\교육운영안.docx
크기: 4.8 MB
SHA-256: ...
```

표에는 발견된 동일 파일을 기록한다.

기준 파일 자기 자신은 결과에서 제외하거나 `기준 파일`로 명확히 표시한다.

권장: 기준 파일은 상단 정보에만 표시하고 결과 행에서는 제외.

---

## 10.3 Summary 시트

폴더 전체 중복 검사의 경우 `Summary` 시트를 추가하면 좋다.

예:

```text
검사 파일수       18,421
중복 그룹            37
중복 파일           104
예상 중복 용량      1.82 GB
건너뛴 파일           12
오류                   3
```

`예상 중복 용량`은 실제 삭제 권고가 아니라 다음 계산값일 뿐이다.

```text
각 중복 그룹에서 한 파일만 남긴다고 가정했을 때의 이론적 용량
```

사용자에게 자동 삭제를 권고하지 않는다.

---

## 10.4 Errors / Skipped

가능하면 기존 Errors 처리 방식을 재사용한다.

예:

| 경로 | 구분 | 내용 |
|---|---|---|
| ... | AccessDenied | 읽기 권한 없음 |
| ... | CloudOnly | 로컬에 내용 없음 |
| ... | ChangedDuringScan | 검사 중 파일 변경됨 |

한 파일의 실패 때문에 전체 작업을 중단하지 않는다.

---

# 11. Excel 결과 UX

기존 File List to Excel 스타일을 그대로 따른다.

필수:

- Excel Table
- AutoFilter
- Header Freeze
- 파일명 또는 별도 열에 Hyperlink
- 크기는 실제 숫자로 저장
- 수정일은 실제 Excel 날짜로 저장
- 그룹 기준으로 정렬 가능

폴더 전체 중복 결과는 기본 정렬을 다음 순서로 권장한다.

```text
중복 절약 가능 크기 내림차순
→ 그룹 번호
→ 상대경로
```

즉 큰 중복부터 쉽게 확인할 수 있게 한다.

---

# 12. 진행률 UI

## 12.1 짧은 작업

빠르게 끝나는 경우 아무 창도 띄우지 않는다.

## 12.2 오래 걸리는 작업

약 700ms~1초 이상 걸릴 것으로 판단되면 작은 Progress UI를 띄운다.

예:

```text
중복 파일 검사 중

파일 찾기          18,421개
후보 추리기         1,320개
Hash 검사             186개

현재:
D:\업무\2026\...\large_file.zip

[취소]
```

단계 표시가 있으면 사용자가 “왜 시간이 걸리는지” 이해하기 쉽다.

---

# 13. 취소 처리

취소 시:

1. 새로운 파일 Hash 작업을 시작하지 않는다.
2. 진행 중인 Stream을 안전하게 닫는다.
3. worker를 종료한다.
4. 임시 workbook을 불완전하게 열지 않는다.
5. Cache DB를 손상시키지 않는다.

부분 결과를 자동으로 열지 않는 것을 기본으로 한다.

향후 “현재까지 결과 열기” 기능은 별도 검토한다.

---

# 14. 검사 중 파일이 변경되는 경우

Hash 계산 전후에 파일 크기와 수정시간을 확인한다.

```text
hash 시작 전 metadata
↓
hash 계산
↓
hash 종료 후 metadata
```

값이 달라졌다면 결과를 신뢰하지 않는다.

해당 파일은:

```text
ChangedDuringScan
```

으로 Errors에 기록하고 중복 그룹에서 제외한다.

---

# 15. 빈 파일(0 byte)

0 byte 파일은 내용이 모두 동일하다.

따라서 Hash 계산 없이 같은 그룹으로 묶을 수 있다.

다만 결과가 너무 많아질 수 있으므로 그룹은 정상 표시하되 기본 정렬에서 뒤쪽에 위치해도 된다.

---

# 16. 설치 / 우클릭 메뉴 통합

기존 설치/삭제 방식을 그대로 확장한다.

새로운 상주 프로그램을 추가하지 않는다.

현재 Explorer 메뉴 구조를 먼저 확인한 뒤 가장 적은 변화로 추가한다.

권장 메뉴 예:

```text
파일 도구 >
    파일 목록을 Excel로
    하위 폴더까지 Excel로
    --------------------
    같은 파일 찾기
    중복 파일 찾기
```

이미 기존 도구가 메뉴를 직접 노출하고 있다면 현재 UX를 크게 바꾸지 말고 동일한 스타일로 기능만 추가한다.

### 표시 조건

- 파일 1개 선택 → `같은 파일 찾기`
- 파일 2개 이상 선택 → `선택 파일 중 중복 찾기`
- 폴더 1개 이상 선택 → `중복 파일 찾기`
- 폴더 내부 빈 공간 → `현재 폴더 중복 파일 찾기` 가능

불가능한 상황에는 메뉴를 숨기거나 비활성화한다.

---

# 17. 기존 코드와의 통합 원칙

작업 시작 전 반드시 기존 프로젝트 구조를 먼저 분석한다.

다음을 우선 재사용한다.

- Explorer selection 전달 방식
- 파일 재귀 탐색 코드
- Progress UI
- Cancel 처리
- Excel workbook 생성기
- Table/Hyperlink 작성 코드
- Errors 시트 생성 코드
- Installer / Uninstaller

중복 기능 때문에 기존 File List 기능을 새로 작성하지 않는다.

공통 부분은 필요 시 작은 모듈로 정리하되 **불필요한 대규모 리팩터링은 하지 않는다.**

---

# 18. 권장 내부 구조

기존 언어와 프로젝트 구조를 우선한다.

개념적으로 다음 정도로 분리한다.

```text
Explorer Command
      ↓
Input Resolver
      ↓
File Enumerator
      ↓
Duplicate Detector
   ├ Size Grouper
   ├ Quick Fingerprint
   ├ Full SHA256
   └ Hash Cache
      ↓
Result Model
      ↓
Existing Excel Writer
```

예시 모델:

```text
DuplicateFile
- absolute_path
- relative_path
- name
- extension
- size_bytes
- modified_at
- quick_hash
- full_sha256
- duplicate_group_id
```

---

# 19. 권장 구현 순서

## Milestone 1 — Core 비교 엔진

Explorer/Excel 없이 먼저 테스트 가능한 함수로 만든다.

구현:

- 파일 크기 grouping
- quick fingerprint
- full SHA-256
- duplicate grouping

CLI 또는 Unit Test로 검증한다.

### 완료 조건

- 같은 내용/다른 파일명 → 같은 그룹
- 같은 크기/다른 내용 → 다른 그룹
- 다른 크기 → Hash 미실행

---

## Milestone 2 — 폴더 재귀 검색

구현:

- 선택 폴더 recursive scan
- reparse point 방지
- access denied 지속 처리
- zero byte
- long/unicode path

---

## Milestone 3 — Hash Cache

구현:

- SQLite cache
- cache hit
- cache miss
- 파일 수정 후 cache invalidation

테스트에서 첫 검사와 두 번째 검사의 Hash 재계산량이 줄어드는지 확인한다.

---

## Milestone 4 — Excel 결과

기존 workbook writer에 연결한다.

구현:

- Duplicates
- Matches
- Summary
- Errors/Skipped
- Hyperlink
- 정렬 가능한 숫자/날짜

---

## Milestone 5 — Explorer 메뉴

구현:

- 단일 파일
- 다중 파일
- 단일/다중 폴더
- 현재 폴더

각 상황에서 필요한 메뉴만 노출되는지 확인한다.

---

## Milestone 6 — Progress / Cancel

대용량 폴더로 실제 테스트한다.

- UI freeze 없음
- 현재 단계 표시
- 취소 가능
- 취소 후 프로세스 잔류 없음

---

## Milestone 7 — 설치/제거 회귀 테스트

기존 installer를 수정한다.

확인:

- 설치 후 기존 파일 목록 기능 정상
- 새 중복 기능 정상
- 제거 후 우클릭 메뉴 잔류 없음
- background process 잔류 없음

---

# 20. 필수 테스트 케이스

## 기능 정확성

1. 동일한 txt 파일 2개
2. 파일명은 다르지만 내용이 같은 파일
3. 크기는 같지만 내용은 다른 파일
4. 크기가 다른 파일
5. 0 byte 파일 여러 개
6. 1MB 이하 파일
7. 수 GB 대용량 파일
8. 한글 파일명
9. 매우 긴 경로
10. 확장자 없는 파일

## 폴더

11. 빈 폴더
12. 하위 폴더 10단계
13. 여러 폴더 선택
14. Junction 포함
15. Symbolic Link 포함
16. Access Denied 포함

## Cache

17. 같은 폴더 두 번 검사
18. 파일 수정 후 재검사
19. 같은 크기로 수정된 파일
20. Cache DB 삭제 후 실행
21. Cache DB 손상 시 안전한 복구 또는 재생성

## 환경

22. OneDrive cloud-only 파일
23. 네트워크 드라이브
24. 외장 SSD
25. Excel이 이미 열린 상태
26. Excel이 설치되지 않은 상태

## 취소

27. scan 단계 취소
28. hash 단계 취소
29. 대용량 파일 hash 도중 취소

---

# 21. 성능 확인 항목

정확한 절대 시간보다 다음을 확인한다.

- 크기가 다른 파일에 Full Hash를 계산하지 않는가?
- Quick Fingerprint로 후보 수가 실제로 줄어드는가?
- 두 번째 실행에서 Cache가 사용되는가?
- 수천 개 파일에서도 UI가 멈추지 않는가?
- 대용량 파일 때문에 메모리 사용량이 급증하지 않는가?
- 네트워크 경로에서 worker 수가 과도하지 않은가?

개발 완료 보고 시 실제 테스트 수치를 함께 남긴다.

예:

```text
Test Folder
- files: 12,438
- total size: 41.7 GB

size candidates: 1,842
quick hash candidates: 326
full hash: 74

duplicate groups: 18
cache hit on 2nd run: 92%
```

수치는 테스트 환경에서 실제 측정한 값만 적는다.

---

# 22. v1에서 하지 않을 것

이번 작업 범위에 포함하지 않는다.

- 자동 삭제
- 자동 보관
- “최신 파일만 남기기” 판단
- 비슷한 파일 찾기
- Word 문서 내용 의미 비교
- 이미지 유사도 비교
- 파일명 유사도 검색
- AI 판단
- Cloud 파일 강제 다운로드
- 백그라운드 전체 PC 인덱싱

**byte 단위로 실제 내용이 같은 파일만** 다룬다.

---

# 23. 완료 기준

다음이 모두 만족되어야 작업 완료로 본다.

- [ ] 기존 File List to Excel 기능이 그대로 정상 동작
- [ ] 단일 파일 `같은 파일 찾기` 동작
- [ ] 폴더 `중복 파일 찾기` 동작
- [ ] 가능하면 다중 파일 선택 비교 동작
- [ ] 크기 → Quick Fingerprint → SHA-256 단계 적용
- [ ] Hash Cache 적용
- [ ] OneDrive cloud-only 강제 다운로드 없음
- [ ] Reparse point 무한 재귀 없음
- [ ] Access Denied 한 건 때문에 전체 작업 중단 없음
- [ ] Progress / Cancel 동작
- [ ] Excel 결과에 Hyperlink 포함
- [ ] Summary / Duplicates(or Matches) / Errors 구조 동작
- [ ] 기존 설치/삭제 정상
- [ ] 제거 후 Explorer 메뉴 잔류 없음
- [ ] 테스트 결과와 변경 파일 목록을 작업 완료 보고서에 남김

---

# 24. 작업 완료 시 보고할 내용

Codex는 구현 후 다음 형식으로 결과를 정리한다.

```text
1. 구현한 기능
2. 변경한 파일
3. 중복 탐지 알고리즘
4. Cache 방식
5. Explorer 메뉴 변화
6. 테스트한 케이스
7. 실제 성능 측정 결과
8. 아직 남아 있는 제한사항
9. 수동으로 확인해야 할 항목
```

테스트하지 않은 내용을 “정상 동작”이라고 추정해서 보고하지 않는다.

---

# 25. 가장 중요한 요약

이 기능의 목적은 거대한 중복 파일 관리 프로그램을 만드는 것이 아니다.

사용자가 파일이나 폴더를 이미 보고 있을 때:

```text
선택
→ 우클릭
→ 같은 파일 찾기
```

만으로 **정확한 중복 파일 목록을 Excel에서 확인**하게 만드는 것이 목적이다.

속도를 위해 모든 파일을 처음부터 Hash하지 말고:

```text
파일 크기
→ 빠른 일부 내용 비교
→ 정말 필요한 파일만 전체 SHA-256
→ 반복 검사는 Cache 사용
```

순서를 지킨다.

그리고 v1에서는 **찾아주기만 하고 원본 파일은 절대 변경하지 않는다.**
