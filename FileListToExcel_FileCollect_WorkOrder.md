# Codex 작업지시 — File List to Excel의 ‘선택한 파일 복사’ Excel 연동 추가

작성일: 2026-09-24 / 작업명세 v1.0
대상 저장소: `prozac0401/File-List-To-Excel`
기능 소속: 기존 File List to Excel의 선택 설치 가능한 Excel 연동
사용자 메뉴: `파일목록 > 선택한 파일 복사…`

> 이 문서를 읽고 기존 코드 조사→구현→실제 테스트→기존 설치기에 통합까지 진행하라. 계획서, VBA 예제, 수동 등록 안내만 제출하지 않는다.
> 여기서 사용자가 말한 ‘Excel에서 저장’은 **목록 통합문서 자체의 Ctrl+S가 아니라, 선택한 행이 가리키는 실제 원본 파일의 복사본을 지정한 폴더에 모으는 기능**이다.

## 1. 제품 경계와 기존 기능 보존

- 탐색기에서 파일목록을 만든다 → Excel에서 필터·정렬한다 → 필요한 행을 고른다 → 실제 파일의 복사본을 모은다.
- 이 기능은 File List to Excel에 속한다. 명단대조기나 Workspace의 선택범위 내보내기를 설치하지 않아도 동작해야 한다.
- 목록 생성/중복 탐지/탐색기 메뉴의 기존 동작을 유지한다. 다른 저장소를 함께 수정하거나 새로운 통합 도구함을 만들지 않는다.
- **기존 명세의 ‘Excel Add-in 설치 금지’는 이번에 사용자가 요청한 선택형 Excel 연동 기능에 한해서 변경한다.** 목록 생성 본체는 여전히 Excel 추가 기능 없이도 실행 가능해야 한다. ADR와 설치 안내에 이 변경을 명시한다.
- Excel 연동을 설치하지 않은 사람이나 결과 파일만 받은 사람은 일반 `.xlsx`로 열람할 수 있다. 결과 파일 안에 실행 매크로를 넣지 않는다.
- 자동으로 할 일은 Excel 메뉴 준비뿐이다. 파일 복사는 사용자가 메뉴를 누르고 목적 폴더를 선택한 뒤에만 시작한다.

## 2. 현재 코드부터 확인

`git status`, `AGENTS.md`가 있으면 그 지침, README, 기존 기능명세, `docs/ARCHITECTURE.md`, `docs/VALIDATION.md`, 솔루션/설치기/테스트/최근 중복찾기 변경을 읽는다.

2026-09-24 조회한 아키텍처 문서의 출발점:

- 네이티브 COM 셸 확장 → 제품 소유 GUID JSON 요청 → 일회성 helper.
- FileScanner와 WorkbookWriter, 직접 ZIP/XML 기반 `.xlsx` 생성.
- 사용자별 WiX MSI, 제품 소유 HKCU 등록, .NET 자체 포함 Windows x64 helper.
- 기존 중복 탐지와 Excel 결과 출력 경로.

이는 착수 단서이며 현재 체크아웃과 다를 수 있다. 실제 클래스/함수/버전은 다시 확인하고 최신 로컬 작업을 덮어쓰지 않는다. README보다 실제 코드와 검증 기록도 함께 확인한다.

## 공통 최우선 조건 — 사용자에게 VBA 실행을 요구하지 않는다

이 절은 기능 구현보다 우선하는 출시 조건이다.

### 자동으로 할 일과 사용자가 할 일을 구분한다

- 사용자는 제품 설치 프로그램을 한 번 실행한다. 설치 시 기능 설명 확인, 설치 동의, 필요한 경우 작업 저장 후 Excel 재시작은 허용한다.
- 설치 프로그램이 해당 제품의 Excel 추가 기능 등록과 정상적인 시작 시 로드 설정을 담당한다.
- 이후 사용자는 평소처럼 Excel을 연다. 메뉴와 이벤트 연결은 자동으로 준비된다.
- **사용자에게 Alt+F8, Alt+F11, VBA 코드 붙여넣기, .bas 가져오기, .vbs 실행, 초기화 매크로 실행, 추가 기능 찾아보기/체크, 매번 런처 실행을 요구하지 않는다.**
- 매번 ‘콘텐츠 사용/매크로 허용’을 눌러야 하는 배포도 합격이 아니다.
- 자동 실행은 ‘메뉴 준비/이벤트 연결’을 뜻한다. 파일을 열거나 셀을 선택했다는 이유만으로 내보내기·복사·저장을 시작하지 않는다. 실제 처리는 사용자가 해당 메뉴를 클릭한 뒤에만 실행한다.
- 개발자의 빌드 작업과 최종 사용자의 설치를 구분한다. 사용자 PC에서 VBA 프로젝트를 조립하거나 `Trust access to the VBA project object model`을 요구하는 방식은 금지한다.

### 구현 방식

1. 기본 후보는 **컴파일된 설치형 Excel COM 추가 기능**이다. 기존 C++/C# 도구 체계를 확인하여 결정한다. 지원되는 VSTO 방식도 검토할 수 있으나 런타임·서명·신뢰·설치 조건을 실제 확인해야 한다.
2. VBA를 꼭 써야 한다는 요청이 아니다. `Workbook_Open` 매크로만 붙여놓고 자동 실행이 완료됐다고 판단하지 않는다.
3. 기존의 VBA 기반 도구를 참고하더라도, 그 배포 방법과 보안 설정 변경을 그대로 복제하지 않는다. 기본 납품 경로는 VBA 실행에 의존하지 않게 한다.
4. 다른 구조가 명백히 더 적절하면 ADR(설계 결정 기록)에 이유와 자동 로드 실증 결과를 남긴다. 수동 매크로 실행을 우회안으로 제시하지 않는다.
5. COM/VSTO를 택해도 조직의 추가 기능 정책, 서명 요구, Office 비활성화 정책은 적용된다. 어떤 PC에서든 무조건 무경고로 실행된다고 주장하지 않는다.

### 보안·설치 조건

- 일반 사용자/HKCU/LocalAppData 설치를 우선한다. 설치기가 제품 소유의 정상적인 COM/추가 기능 등록을 자동으로 수행하는 것은 허용한다.
- 매크로 보안을 낮추거나, ‘모든 매크로 허용’, 광범위한 신뢰 위치, Trust Center/그룹 정책 완화, MOTW 일괄 해제, 임의 인증서 신뢰 등록으로 무조작 요구를 충족시키지 않는다.
- 사내 정책/서명/필수 런타임 때문에 불가능한 환경에서는 원인과 필요한 승인 조건을 진단한다. 기능 불가를 숨기고 ‘설치 성공’으로 끝내지 않는다.
- 사용자가 비활성화한 추가 기능이나 Office가 문제로 차단한 추가 기능을 감시하며 강제로 재활성화하지 않는다.
- 서명 키/인증서는 하드코딩하거나 저장소에 올리지 않는다. 서명되지 않은 평가 빌드와 배포 승인된 빌드를 구분한다.
- 기존 Excel 창을 강제 종료하거나 저장하지 않은 파일을 자동 저장하지 않는다. 설치·업데이트·제거에 재시작이 필요하면 저장 후 닫도록 한 번 안내한다.
- 최종 사용자가 별도 Python/개발 SDK/스크립트 실행 정책을 설정하지 않게 패키징한다.

### Excel 메뉴 수명주기와 공존

- 제품별 COM CLSID/ProgID, 메뉴 Tag/ID, 설치 폴더, 설정, 제거 소유권을 분리한다.
- 시작 시 동일 메뉴/이벤트가 중복 등록되지 않게 한다. 종료·연결 해제 시 자신이 만든 컨트롤과 이벤트만 해제한다.
- Office의 기본 메뉴나 명단대조기 등 다른 제품 메뉴를 초기화/전체 삭제하지 않는다. `CommandBars.Reset` 방식은 금지한다.
- `CommandBars`를 사용하면 자신의 컨트롤에 고유 Tag와 적절한 임시 수명을 적용한다. Ribbon/컨텍스트 메뉴 방식을 선택해도 같은 소유권 원칙을 지킨다.
- 메뉴 클릭을 컴파일된 콜백에 연결한다. 존재하지 않는 VBA 매크로 이름을 `OnAction`에 넣는 껍데기만 구현하지 않는다.
- Excel Application/Workbook/Worksheet/Selection은 해당 추가 기능이 연결된 **현재 Excel 인스턴스**에서 얻는다. 임의 `GetActiveObject` 하나로 여러 Excel 프로세스 중 대상을 추측하지 않는다.
- 우클릭 이벤트의 Target은 마우스 근처 셀이지 전체 선택범위가 아니다. 실제 Selection과 교차 관계를 확인한다. 선택 안을 우클릭했을 때 여러 셀 선택을 한 셀로 잃지 않는지 시험한다.
- 메뉴 표시 시 파일 내용 읽기·네트워크 검사·대규모 계산을 하지 않는다. 무거운 검사는 명령 실행 후에 한다.
- Excel COM 객체는 허용되는 UI/STA 문맥에서 다룬다. 백그라운드 작업에는 값과 경로를 복사한 DTO만 전달한다.
- Excel이 종료된 뒤 남는 전용 상주 서비스/감시 프로세스를 두지 않는다. 작업용 helper는 해당 작업이 끝나면 종료한다.

### 가장 먼저 통과할 자동 로드 시험

깨끗한 Windows 사용자 프로필 또는 격리된 테스트 VM에서 진행한다.

1. VBA 프로젝트 접근 신뢰는 꺼두고, 문서 VBA 매크로를 허용하지 않은 상태를 유지한다. 단, 설치형 추가 기능 자체는 조직 정책상 허용된 시험 환경이어야 한다.
2. 완성된 설치 프로그램만 실행한다. VBE/매크로 실행/추가 기능 수동 체크를 하지 않는다.
3. Excel을 열고 해당 컨텍스트 메뉴가 준비되는지, 실제 메뉴 클릭이 기능으로 이어지는지 확인한다.
4. Excel을 완전히 닫았다가 다시 연다. Windows 재로그인/재부팅 후에도 같은 시험을 한다.
5. 서로 다른 Excel 프로세스 두 개, 여러 통합문서, 기존 명단대조기와의 동시 설치를 시험한다.
6. 설치·업그레이드·제거 이후 중복 메뉴, 유령 메뉴, 다른 도구 삭제, 남는 helper가 없는지 확인한다.
7. 정책 차단, 비트수 불일치, 누락 런타임, 로드 실패는 각각 다른 진단으로 보고한다.

**개발용 Excel에서 `Run`을 눌러 성공한 것은 자동 로드 시험을 통과한 것이 아니다. Windows/Excel 환경이 없으면 해당 시험은 미실행으로 남기고, 소스/단위시험/설치 패키지 검사와 구분한다.**

## 3. 정상 사용자 흐름

```text
File List to Excel 설치 시 ‘Excel에서 선택한 파일 복사’ 구성 요소 포함
→ 평소처럼 탐색기에서 파일목록 생성
→ 결과 Excel에서 필터/정렬
→ 필요한 행의 셀 선택 (파일명 열만 선택해도 됨)
→ 셀 우클릭 → 파일목록 > 선택한 파일 복사…
→ 목적 폴더 한 번 지정
→ 목적 폴더 아래 새 ‘모은파일_날짜_시간_식별자’ 폴더에 복사
→ 완료/제외/실패 수와 결과 폴더 표시
```

- 사용자가 전체경로 열을 복사하거나 탐색기로 왕복할 필요가 없다.
- 정상적인 소량 복사는 별도 옵션/재확인 창을 연속해서 띄우지 않는다.
- 목적 폴더 선택 화면에 선택 파일 수/예상 크기/제외 예정 항목을 함께 보여준다. 마지막 확인 버튼은 ‘여기에 복사’처럼 실제 동작을 표시한다.
- 복사본은 목적 폴더 바로 아래가 아니라 이번 작업용 새 하위 폴더에 넣는다. 기존 전달물과 섞지 않는다.
- File List 결과를 열었다는 이유만으로 경로 존재 확인·네트워크 접속·복사를 시작하지 않는다.

## 4. ‘File List 결과’ 인식 계약

### 자동으로 생성하는 식별 정보

사용자는 표식/설정을 작성하지 않는다. 기존 WorkbookWriter가 새 결과를 만들 때 자동으로 넣는다.

최소 예시(기존 이름과 충돌하면 동일 목적의 namespace로 조정):

```text
통합문서의 짧은 사용자 지정 속성
  FLT.Product = FileListToExcel
  FLT.ActionSchemaVersion = 1
  FLT.WorkbookId = UUID

복사 가능한 결과표
  제품이 등록한 고유 Table 이름
  숨긴 기술 열: __FLT_ItemId
  숨긴 기술 열: __FLT_SourceRecord
```

- 사용자 지정 속성에는 짧은 식별자/버전만 넣는다. 전체 파일 목록 JSON을 속성 한 개에 넣지 않는다. [MS-I]
- 기존 Table 이름을 불필요하게 바꾸지 않는다. 실제 표 이름/필수 열/역할을 작은 제품 메타정보로 등록하거나 버전별 고정 규칙으로 식별한다.
- `__FLT_ItemId`: 목록 항목별 고유 ID.
- `__FLT_SourceRecord`: `kind`, 정확한 `absolutePath`, `sizeBytes`, `modifiedUtcTicks` 등의 생성 당시 정보를 담은 bounded JSON 문자열. 큰 정수/UTC ticks는 Excel 정밀도 손실을 피하도록 문자열로 기록한다.
- 숨긴 기술 열은 **동일 Excel Table의 행 일부**여야 한다. 별도 시트의 고정 행 번호와 연결하면 정렬 후 다른 파일을 가리킬 수 있으므로 금지한다.
- 전 항목의 기술 정보는 조회 전용 참고 자료다. 사용자에게 편집을 요구하지 않는다.
- 기술 문자열이 Excel 셀 길이 제한을 넘으면 조용히 자르지 않는다. 그 항목을 복사 불가로 명시하고 기존 목록 조회는 유지한다.
- 이런 메타데이터는 수작업 수정 오류를 줄이는 **형식 식별 정보**이지 서명/위조 방지/출처 인증이 아니다. 일치한다고 안전한 파일로 신뢰하지 않는다.

### 인식 규칙

1. 현재 통합문서의 제품/지원 버전/WorkbookId 확인.
2. 선택이 제품이 생성한 복사 가능한 Table의 DataBodyRange에 속하는지 확인.
3. 필수 열과 선택 행의 ItemId/SourceRecord가 구조적으로 유효한지 확인.
4. 표시된 `전체경로`와 SourceRecord의 경로가 일치하는지 확인. 열 위치가 바뀌어도 이름/제품 필드 매핑으로 읽는다.
5. 최종적으로 실제 파일 존재/종류/접근 가능성은 명령 실행 후 복사 엔진에서 검증한다.

### 오인하지 말 것

- 파일명, 시트명이 ‘Files’라는 사실, `전체경로` 헤더 하나만으로 인식하지 않는다.
- 일반 Excel에 비슷한 표를 만들거나 File List 행을 복사해 붙였다고 자동으로 파일 작업 권한을 부여하지 않는다.
- 지원하지 않는 과거 결과/미래 버전/메타데이터 제거 파일에서는 비활성화하거나 새 목록 생성이 필요하다고 안내한다. 추정해서 승격하거나 원본 Excel에 표식을 자동 삽입하지 않는다.
- Save As로 이름/폴더만 바꾸어 메타데이터와 표가 유지되면 사용 가능해야 한다.
- 열 재배치·필터·정상 Table 정렬·시트 이름 변경은 지원한다. Table/필수 열 이름 변경, 기술 열 삭제, 부분 열 정렬/붙여넣기로 레코드가 손상된 경우 안전하게 거절한다.
- 일반적인 조회표 데이터가 수정되었다고 저장을 강제하지 않는다. **현재 열려 있는 Excel 메모리의 선택과 표 상태**를 기준으로 읽는다. 디스크에 마지막 저장된 `.xlsx`만 다시 읽는 구현은 금지한다.

### 적용할 결과표

- Files/Files_2… 등 일반 파일목록 표는 우선 지원한다.
- 현재 구현의 Duplicates/Matches 등 개별 파일을 가리키는 표에도 같은 행 계약을 적용한다.
- Summary/Errors/Skipped 등 통계·오류표에는 파일 복사 명령을 활성화하지 않는다.
- 이전 생성 결과는 조회 기능을 그대로 유지한다. 신규 기능을 쓰기 위해 결과 파일 자체에 매크로를 넣지 않는다.

## 5. 선택 해석

- 현재 활성 워크시트의 **한 제품 Table**을 대상으로 한다.
- 선택 영역과 DataBodyRange의 교차에서 행을 구한다. 파일명 셀 하나를 선택해도 같은 행의 실제 원본 경로를 얻는다.
- Ctrl로 여러 행/셀을 선택한 경우 지원한다. 같은 행을 여러 셀에서 선택해도 한 번만 처리한다.
- 필터로 가린 행과 수동으로 숨긴 행은 제외한다. ‘보임’은 화면 스크롤 위치가 아니라 행 숨김 여부다.
- 전체 행을 선택했다면 먼저 표와 교차를 구한다. 전체 워크시트를 순회하지 않는다.
- 헤더/합계만 선택했으면 처리할 파일 없음으로 안내한다. 복수 표나 관계없는 범위가 섞인 선택은 임의로 합치지 말고 이유를 안내한다.
- 폴더 행, 링크/정션, 비파일 항목은 제외하고 수를 표시한다. 폴더 선택을 하위 파일 전체 복사로 확대하지 않는다.
- 같은 원본 경로가 반복되면 한 번 복사하고 중복 선택 수를 알린다. 다른 경로의 파일은 내용이 같아도 둘 다 복사한다. 이 기능에서 중복 해시 탐지는 하지 않는다.
- 사용자가 고른 파일목록 행의 순서에 따라 충돌 이름을 결정하는 등 결과를 재현 가능하게 만든다.

## 6. 기존 helper와의 연결

권장 구조:

```text
Excel 시작 시 로드되는 얇은 컴파일 추가 기능
  → 메뉴 상태/선택 캡처/형식 확인
  → 현재 선택 행을 불변 요청 DTO로 변환
  → 제품 소유 GUID 요청 파일 또는 제한된 IPC
  → 기존 FileListToExcel helper의 새 collect-files 동작
  → 사전 검증/목적지 선택/진행률/복사/내부용 결과 기록
```

- Explorer 셸 COM DLL과 Excel 추가 기능의 CLSID/ProgID/역할은 분리한다. 같은 COM 객체인 것처럼 하나의 등록을 재사용하지 않는다.
- 기존 요청 파일 검증/일회성 소비 패턴을 재사용하되, 신규 동작과 DTO 버전을 구분한다. 기존 list/matches/duplicates/duplicate-files 의미를 바꾸지 않는다.
- 파일 경로를 명령줄로 대량 나열하거나 `cmd /c`, PowerShell 명령문으로 이어붙이지 않는다.
- helper 요청은 제품 소유 경로/GUID 파일/최대 바이트/허용 모드/항목 수/절대 경로를 검사한다. 자기 요청만 정리한다. reparse 요청파일·임의 경로 삭제를 막는다.
- SourceRecord JSON은 코드가 아니다. 명령/URL/매크로/함수로 실행하지 않는다.
- helper에는 Excel COM 객체를 전달하지 않는다. Excel이 대기 없이 작업을 넘기더라도 요청의 데이터는 더 이상 Selection 변화에 영향받지 않아야 한다.
- 정상 UI에서는 동일 Excel 인스턴스의 중복 클릭을 막고, 여러 인스턴스/작업은 각각 별도 목적 하위 폴더와 요청 ID로 격리한다.

## 7. 복사 엔진의 확정 정책

### 사전 검증

- 허용: 사용자에게 접근 권한이 있는 로컬 파일/지원되는 UNC 일반 파일 경로.
- 불허: 상대경로, 임의 URL, 명령문, 장치 경로 악용, 폴더 재귀, alternate stream을 별도 원본처럼 지시하는 경로.
- 단순히 모든 `:`를 막아 드라이브 문자를 깨지 말고 Windows 경로 규칙으로 검사한다. 지원하지 않는 특수 경로는 이유와 함께 제외한다.
- SourceRecord와 현재 파일의 크기·UTC 수정 정보가 달라졌으면 기본적으로 제외하고 ‘목록 생성 후 변경됨’으로 알린다. 비슷한 파일을 찾아 대체하지 않는다.
- 크기/수정시각이 같다고 목록 생성 당시 바이트까지 증명되는 것은 아니다. 이 기능은 과거 버전 복원/완전한 시점 스냅샷 기능이 아니다.
- 클라우드 전용/recall이 필요한 파일은 기본적으로 제외한다. 검색/선택만 했다는 이유로 강제 다운로드하지 않는다. 기존 no-recall 검증 코드를 우선 검토한다.
- 원본/중간 경로의 링크·정션 및 검증 중 경로 바뀜은 처리 직전에도 확인한다. 단순 사전 문자열 검사만으로 실제 복사 대상을 확정하지 않는다.
- DRM/암호화/보호 정책은 우회하지 않는다. 허용된 파일 복사가 차단되면 실패로 보고한다.

### 복사

1. 목적 폴더는 사용자가 직접 지정한다. 같은 위치에 충돌하지 않는 새 하위 폴더를 생성한다.
2. 파일은 한 단계 평평한 구조로 모은다. 원래 폴더 전체 구조를 재현하는 옵션은 v1 범위 밖이다.
3. 같은 이름 충돌 시 `이름 (2).확장자`처럼 복사본 이름만 구분한다. 기존 파일을 덮어쓰지 않는다. 실제 파일 생성에서도 no-overwrite를 강제해 경쟁 조건을 막는다.
4. 원본은 읽기/복사만 한다. 이동, 원본 이름 변경, 삭제, ACL 수정은 하지 않는다.
5. OS 지원 복사 API의 진행률/취소/no-overwrite를 활용한다. CopyFileExW는 후보이며 필요 속성 보존과 cloud/링크 안전성은 별도로 검증한다. [MS-J]
6. 암호화된 파일을 평문으로 내보내는 옵션을 자동으로 사용하지 않는다. 보안 관련 속성/ADS/MOTW를 지우는 ‘정리’를 추가하지 않는다. 대상 파일시스템의 보존 제약은 진단한다.
7. 성공·실패·취소·제외를 파일별로 기록한다. 한 파일 실패가 전체 파일 복사를 조용히 누락시키지 않게 한다.
8. 복사 전후 동일 파일의 크기·시간/가능한 파일 식별자를 재확인한다. 작업 중 변경이 확인되면 성공으로 분류하지 않는다. 자체 생성한 미완료 출력만 안전하게 정리한다.
9. 복사 후 원본 파일 내용 확인을 위해 Office 문서를 자동으로 열거나 매크로를 실행하지 않는다.

### 취소·진행률

- 현재 파일명, 완료 개수, 실제 전송량을 가능한 범위에서 표시한다. 큰 파일도 UI가 멈춘 것처럼 보이지 않게 한다.
- 취소하면 새 파일을 시작하지 않고 진행 중 복사를 취소한다. 완료된 복사본은 유지하고, 자신이 만든 미완료 파일만 정리한다.
- 네트워크/OS 호출 중 취소 반영 지연을 숨기지 않는다. 불필요하게 Excel 프로세스를 종료하지 않는다.
- 기본 동시 복사는 1개로 시작한다. 병렬화는 측정 후 정한다. 속도 향상을 이유로 안전 검증을 생략하지 않는다.
- 초기 한도: 고유 선택 파일 10,000개 및 기존 요청 크기 한도 이내. 기존 시스템 한도가 더 작으면 작은 값을 사용한다. 목록 전체 생성 한도를 이 기능 때문에 낮추지 않는다.
- 실패/취소 뒤 자동 재실행하지 않는다. 다시 실행하면 새 작업 폴더에 복사하여 이전 결과를 덮지 않는다.

## 8. 결과와 개인정보

기본 완료 안내:

```text
15개 중 13개 복사 완료
1개: 원본을 찾지 못함
1개: 목록 생성 후 변경됨
[모은 폴더 열기] [작업 결과 보기]
```

- 전달용 폴더에는 복사한 파일만 둔다. 내부 원본 전체경로를 담은 `복사결과.xlsx`를 자동으로 함께 넣지 않는다.
- 상세 결과는 제품 LocalAppData의 Reports/Logs에 로컬로 보관하고 요청 시 보여준다. 원본/목적지/성공·실패 사유/명칭 변경 대응을 담는다.
- 상세 Excel 보고서가 필요하면 기존 WorkbookWriter를 재사용한다. 원본 경로 등 모든 문자열은 수식이 아니라 리터럴로 기록한다.
- 보고서/로그는 외부 전송하지 않는다. 공개 이슈/릴리스에 사용자 경로를 그대로 넣지 않는다.
- 결과 폴더 열기는 사용자가 선택한 목적 폴더의 이번 하위 폴더만 연다. 복사한 개별 문서/실행파일을 자동 실행하지 않는다.
- 목록 파일과 실제 파일은 별개다. 다른 PC에서 원본에 접근할 수 없다면 목록만 보고 복사할 수 없음을 안내한다.

## 9. 설치와 사용자 기대

기존 설치 패키지에 별도 구성 요소를 추가한다.

```text
File List to Excel
  [필수] 탐색기 파일목록/중복 찾기
  [선택] Excel에서 목록의 선택 파일 복사
```

- 기능 설명에 ‘Excel 우클릭 메뉴를 추가하며 실제 파일의 복사본을 만든다’를 명시한다.
- 신규 설치 기본 선택 여부는 ADR로 결정하고 설치 화면에 명시한다. 기존 사용자의 무인 업그레이드에서 몰래 구성 요소를 켜지 않는다.
- 선택된 구성 요소의 추가 기능 등록/로드 설정은 설치기가 수행한다. 사용자가 Excel 추가 기능 메뉴에서 따로 찾아 등록하게 하지 않는다.
- MSI의 기존 UpgradeCode/설치 경로/셸 등록 소유권을 존중하고 신규 Excel 연동의 고유 등록을 추가한다. 설치기가 소유하지 않은 원본·결과 Excel·모은 파일은 제거하지 않는다.
- 기존 x64 Explorer 확장과 Excel 비트수는 별개다. Windows x64 + Excel x86/x64 추가 기능 빌드를 구분한다. x86 shim에서 x64 helper로 작업을 넘기는 경우 IPC와 설치를 실제 시험한다.
- Excel 미설치/대체 앱 환경에서는 본체 파일목록 생성은 유지하고 Excel 연동은 지원 불가로 명시한다.
- 선택범위 내보내기/명단대조기와 COM/메뉴/설치 키를 공유 소유하지 않는다. 어느 하나를 제거해도 나머지가 동작해야 한다.

## 10. 구현 순서

| 단계 | 작업 | 통과 조건 |
|---|---|---|
| M0 | 현재 코드/설치/명세와 충돌 확인, ADR | 본체 보존 + 선택형 Excel 연동 경계 확정 |
| M1 | 최소 Excel 추가 기능과 MSI 구성 요소 | 설치만으로 다음 Excel 시작 시 자동 메뉴/콜백 |
| M2 | 결과표 식별 메타정보/행 계약 | 정렬·필터·Save As 후 정확한 원본 대응 |
| M3 | 선택 캡처/요청 검증 | 저장 전 Excel 상태 기준, 일반 표 오인 없음 |
| M4 | CopyPlanner/CopyService/보고서 | 원본 무변경, 충돌·실패·취소 안전 |
| M5 | 실제 Excel→helper 통합 | 행 선택→목적지 한 번→실제 복사 |
| M6 | 공존/업그레이드/비트수/회귀 | 기존 목록·중복찾기·다른 추가 기능 정상 |
| M7 | 산출물/문서/검증 기록 | 설치 패키지와 실측/미검증 보고 |

## 11. 필수 수용 시험

### 표 인식과 선택

- 새 Files, Files_2, Duplicates, Matches 중 실제 존재하는 각 제품 표.
- 일반 Excel, 같은 헤더만 만든 위장 표, 과거 표식 없는 결과, 미래 버전은 무동작.
- 필터로 100개 중 7개만 보임: 해당 7개만 복사.
- 수동 숨김 행 제외; 파일명 열 선택만으로 올바른 원본 선택.
- 정렬/열 재배치/시트 이름 변경/Save As/미저장 필터 상태.
- Ctrl 다중 행, 겹친 선택, 전체 행 선택의 표 교차, 헤더·합계만 선택.
- 복수 표 혼합, 기술 열 삭제, ItemId 중복/JSON 손상, 경로 열 부분 수정은 안전하게 거절/제외.
- 복사한 File List의 보이는 셀만 다른 통합문서로 옮기면 자동 활성화되지 않음.

### 실제 파일

- 같은 이름·다른 폴더의 파일 여러 개: 모두 보존되고 이름만 구분.
- 같은 원본 경로 반복: 한 번 복사. 내용 동일·다른 경로: 둘 다 복사.
- 0바이트, 확장자 없음, 한글/공백/긴 경로/수식 모양 이름.
- 폴더 행, symlink/junction, cloud-only, 삭제된 원본, 권한 거부, 네트워크 끊김.
- 목록 생성 후 수정된 파일 / 복사 중 수정·교체된 파일.
- 저장 공간 부족, 목적지 접근 거부, 이름 경쟁 충돌, 중복 실행.
- 큰 파일 취소: 완료본 유지, 자체 미완료본 정리, 원본 무변경.
- 복사 결과 보고서가 전달용 폴더 안에 자동 포함되지 않음.
- UNC 목적지는 사용자가 명시적으로 선택한 경우만 사용. 자동으로 외부 위치를 만들지 않음.

### 설치·회귀

- 사용자 VBA action 없이 설치→Excel 시작→메뉴→실제 파일 복사.
- 완전히 종료 후 재시작/재로그인/재부팅에도 자동 로드.
- 서로 다른 Excel 프로세스와 다른 제품의 동시 설치.
- 목록 본체만 설치 / Excel 연동까지 설치 / Excel 미설치에서 본체 사용.
- 명단대조기나 선택범위 내보내기 제거 후 파일목록 복사 유지, 반대 순서도 시험.
- 기존 파일목록의 열/타입/링크/시트 분할/오류/취소/no-open/설치·제거 회귀.
- 기존 중복 탐지·캐시·cloud/no-recall 안전성 회귀.
- 추가한 기술 메타정보 때문에 목록 생성 성능/호환성이 과도하게 나빠지지 않음.

## 12. 완료 보고와 납품

납품: 기존 프로젝트에 통합된 소스/테스트, 자동 로드 Excel 추가 기능, 업그레이드 가능한 설치 패키지, 메뉴/비트수/선택 구성 요소 안내, 스키마/마이그레이션 정책, 실제 검증 기록.

완료 보고:

1. 수정 파일, 재사용한 코드, 기존 동작에 생긴 명시적 변경.
2. 자동 로드 구조와 설치 소유권, 사용자에게 실제로 요구한 행동.
3. File List 인식/선택 행-원본 대응 방법과 위조 방지 기능이 아니라는 경계.
4. 실제 Excel 버전/비트수에서의 설치·재시작·파일 복사 증거.
5. 필터/정렬/다중 선택/충돌/취소/원본 무변경 시험 결과.
6. 원본 보호·cloud·링크·보고서 개인정보 처리와 미검증 제약.
7. 기존 목록/중복찾기/다른 도구 회귀 결과.
8. 빌드 산출물, 서명·배포 승인 필요 사항, 미실행 항목.

테스트하지 않은 환경을 ‘정상’으로 추정하지 않는다. 배포·푸시는 별도 권한/요청에 따른다.

## 13. 확인 자료

- 대상 저장소: https://github.com/prozac0401/File-List-To-Excel
- 기존 아키텍처: https://github.com/prozac0401/File-List-To-Excel/blob/main/docs/ARCHITECTURE.md
- 기존 안내: https://github.com/prozac0401/File-List-To-Excel/blob/main/README.md
- [MS-A] Excel/VSTO 시작 시 로드와 사용자별 등록: https://learn.microsoft.com/en-us/visualstudio/vsto/registry-entries-for-vsto-add-ins?view=visualstudio
- [MS-B] Office 인터넷 출처 매크로 차단과 Excel 추가 기능의 제약: https://learn.microsoft.com/en-us/microsoft-365-apps/security/internet-macros-blocked
- [MS-C] 우클릭 이벤트와 Target 의미: https://learn.microsoft.com/en-us/office/vba/api/excel.application.sheetbeforerightclick
- [MS-D] 컨트롤 추가와 임시 수명: https://learn.microsoft.com/en-us/office/vba/api/office.commandbarcontrols.add

문서 주소에 `/vba/api/`가 있어도 여기서 참조하는 것은 Excel/Office 객체 모델이다. 사용자가 VBA를 직접 실행해야 한다는 뜻이 아니다. VSTO 전용 등록 문서를 네이티브 COM 등록 절차에 그대로 대입하지 말고 선택한 구현의 공식 배포 방법을 별도 검증하라.

- [MS-I] Workbook.CustomDocumentProperties 및 짧은 속성값 제약: https://learn.microsoft.com/en-us/office/vba/api/excel.workbook.customdocumentproperties
- [MS-J] CopyFileExW 진행률/취소/덮어쓰기 방지/속성 처리: https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-copyfileexw

표식의 존재는 출처 진위를 증명하지 않는다. OS API의 존재도 조직 정책을 통과한다는 보장이 아니다. 실제 대상 환경에서 검증하고, 불가능한 조건은 승인 필요/미지원으로 정확하게 표시한다.
